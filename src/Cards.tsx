import { FlexProps, Wrap, WrapItem, WrapProps } from "@chakra-ui/react";
import * as React from "react";

// const Cards: React.FC<FlexProps> = (props) => (
//   <Flex>
//     <Box>
//       <Box as="figure">
//         <Image>
// );

export default Cards;
